# TASKS

現状の実装済み範囲は[README.md](./README.md)を参照。ここには残っているスコープを優先度別に記録する。

## 優先度：高

### セキュリティ（マルチテナント化に伴い発見）
- [x] `createReservationAction`/`updateReservationAction`/`deleteReservationAction`（[actions.ts](./src/app/actions.ts)）が、対象の会議室・予約が操作者の所属組織のものかを検証していない。組織が1つしかなかった頃は問題が表面化しなかったが、複数組織が存在する今は、他組織のroomId/予約IDを直接指定されると操作できてしまう可能性がある。`saveFloorLayoutAction`・フロア図アップロードAPIと同じ「組織所有チェック」パターンを追加する必要がある
  - `isRoomInOrganization`（rooms→floors→buildings）・`isReservationInOrganization`（reservations→rooms→floors→buildings）を追加し、3つのActionそれぞれで書き込み前に検証するよう修正。テストはDB結合が必要でこのリポジトリに結合テストの仕組みがまだ無いため追加せず（`saveFloorLayoutAction`の既存の組織所有チェックにもテストが無いのと同様）。tsc/eslint/vitest/buildはすべて成功を確認済み

### デプロイ（ポートフォリオとして公開するために必須）
- [x] Vercelへデプロイ：https://meeting-room-reservation-theta.vercel.app/login
- [x] 環境変数（`DATABASE_URL` / `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` / `SUPABASE_SECRET_KEY`）をVercel側に設定
- [x] デプロイ後の動作確認（サインイン→フロアマップ表示まで確認済み）
- [x] `@types/node`のバージョン不整合でVercelビルドが失敗する問題を修正（ローカルの`--legacy-peer-deps`頼みだった箇所を根本修正）

### 認証・認可（設計は[README.md](./README.md#3-設計判断)で整理済み。上流設計の見せ場として実装した）
- [x] **Auth.js（Google OAuthのみ）へ移行完了**：`feature/authjs-google-auth`ブランチで実装。Supabase Auth（メール+パスワード／Google OAuth移行）は2回とも設定依存・原因不明の不具合で頓挫したため、認証をAuth.jsに切り出し、認可（role・organization）は自前の`users`テーブルにそのまま残す構成に変更した（詳細はREADMEの「設計判断の経緯」を参照）
  - [x] `src/auth.ts`（Auth.js設定、Google Provider、JWTセッション、`signIn`/`jwt`/`session`コールバックで`users`テーブルと突き合わせ）
  - [x] `getAuthContext()`をAuth.jsの`auth()`ベースに書き換え（呼び出し側のインターフェースは維持、無改修で動作）
  - [x] 招待＝`users`テーブルへの許可リスト登録のみに簡略化（メール送信は行わない。本人はURLを伝えられて「Googleでサインイン」を押すだけ）
  - [x] 旧Supabase Auth関連コード（`/login`のパスワードフォーム、`/set-password`、`/auth/callback`、`browserClient.ts`/`serverAuthClient.ts`、`proxy.ts`）を削除
  - [x] ローカルでGoogle Cloud ConsoleのリダイレクトURI登録・`.env`のAuth.js関連値設定・実機での動作確認（サインイン→フロアマップ表示まで確認済み）
  - [x] **バグ修正**：`users.authUserId`が`uuid`型のままだったため、Googleの`sub`（数値文字列、UUID形式ではない）を書き込もうとしてPostgresエラーになりサインインが失敗する問題。`text`型に変更して解決（[schema.ts](./src/lib/db/schema.ts)）
  - [x] **要設定**：Google Cloud ConsoleのOAuth同意画面が「テスト」公開ステータスのままだと、テストユーザー未登録のGoogleアカウントは同意画面で401になり弾かれる問題に気づき、「本番」公開ステータスに変更して解決（スコープがemail/profile/openidのみのため審査不要）
  - 招待フローの改善案（ログインURLのコピーボタン、複数メールアドレスの一括招待）は一度実装したが、ログインURLはアプリのURLと同じで招待ごとに変わらないため「コピー機能」自体が不要と判断し撤回。一括登録も現時点では不要と判断し、1件ずつの入力に戻した
- [x] 本番（Vercel）側でのGoogle Cloud ConsoleリダイレクトURI登録・環境変数設定・動作確認まで完了
- [ ] （保留）Google以外のサインイン手段（フォールバック）。まずはGoogleのみで安定動作を確認してから要否を判断する

### 組織のセルフサインアップ（`feature/owner-signup`ブランチで実装。管理者を増やすたびに再デプロイが必要なconfigシード方式の限界に対応）
- [x] **重大バグ修正**：`getFloorMapData()`が「DB内の先頭の組織」を無条件に採用していた問題。組織が1つしかない前提が崩れる（＝2つ目の組織がサインアップした瞬間に他組織のデータが見えてしまう）ため、ログイン中メンバーの`organizationId`で必ず絞り込むよう修正（[getFloorMapData.ts](./src/lib/queries/getFloorMapData.ts)）
- [x] `/signup`：組織名を入力→「Googleでサインアップ」。組織名は短命Cookie（[ownerSignup.ts](./src/lib/auth/ownerSignup.ts)）でGoogle OAuthの往復を橋渡しし、サインイン完了時（`auth.ts`の`jwt`コールバック）に`organizations`行・`users`行（role: admin）を作成する
- [x] オンボーディング画面：組織に建物が1つも無い状態でログインすると、管理者には「最初の建物・フロアを登録」フォーム（[CreateFirstBuildingForm.tsx](./src/components/CreateFirstBuildingForm.tsx)）、一般メンバーには「管理者の設定待ち」メッセージを表示
- [x] **バグ修正**：`/signup`で既に別組織に所属（招待中も含む）済みのメールアドレスを使うと、新規組織作成の意図を無視して既存組織にログインしてしまう問題。「1アカウント=1組織」の設計を明確化し、既存メンバーなら`/signup?error=AlreadyMember`へリダイレクトして拒否するよう修正（`signIn`コールバックの判定順序の問題だった）
- [ ] 会議室（room）自体の追加UIは別タスク（「新規会議室の追加・削除をUIから行えるようにする」）のまま。オンボーディング直後は建物・フロアはあっても部屋が無い状態になる
- 既存の`config/seed-admins.json`によるconfigシード方式は、最初のデモ組織の管理者ブートストラップとして引き続き利用可能（廃止していない）

<details>
<summary>旧実装（Supabase Auth、履歴として保持）</summary>

- [x] Supabase Authのセットアップ（初回：メールリンク→パスワード設定／2回目以降：メール+パスワード）
- [x] 招待発行API・管理ページ（`/admin/invitations`、管理者が招待メールアドレス＋roleを指定）
- [x] 招待メールの自動送信（`auth.admin.inviteUserByEmail`）。旧・自前トークン方式(`/invite/[token]`)は廃止し、`invitations`テーブルも削除
- [x] 招待メール経由のサインイン・組織参加フロー（メールのリンク→`/auth/callback`→`getAuthContext()`が自動でusers行に紐付け→`/set-password`）
- [x] admin/member権限に応じたAPIの認可チェック（フロア図アップロードは管理者のみ、予約作成はサインイン済み組織メンバーのみ）
- [x] **バグ修正**：Supabase管理API発行リンクの認証情報がURLハッシュで返る問題。`/auth/callback`をサーバーRoute Handler→クライアントページに書き換えて対応
- [x] **要設定**：SupabaseダッシュボードのRedirect URLsが未設定だとサインインが失敗する問題に気づき、READMEのセットアップ手順に追記
- 未解決のまま終了：パスワード再設定専用の導線、Supabaseのメール送信レート制限対策、Google OAuth移行時のPKCE code verifier不具合

</details>

## 優先度：中

### 予約編集・楽観ロックUI
- [x] 予約編集フォーム（会議名・予約者名・時間の変更）
- [x] 楽観ロック競合時のUI（「他のユーザーによって更新されています」の表示＋「最新の内容を読み込み直す」導線）。ブラウザ+DB直接操作で競合を再現し実機確認済み
- [x] 予約削除機能（楽観ロック付き、2段階確認UI）

### フロア図（背景画像）編集画面
- [x] `floors`テーブルに`floorPlanImagePath`/`Width`/`Height`を追加、Supabase Storage（`floor-plans`バケット）連携
- [x] フロア図アップロードAPI（`/api/floors/[floorId]/image`）と暫定アップロードUI
- [x] フロアマップSVGの背景に画像を表示（座標系を画像ピクセルサイズに合わせる）
- [x] 会議室をドラッグして配置できる編集画面（管理者向け「編集モード」トグル、フロアマップ画面に統合）。ドラッグ位置はローカルに保持し「保存」ボタンでまとめて反映する方式。実機確認済み
- [x] **会議室の追加・削除UI**（`feature/room-management`ブランチ）：編集モードに「＋ 会議室を追加」ボタンを追加し、名前・定員を入力してドラッグ配置、既存の会議室は右上の「×」で削除予定にできる（元に戻す可）。すべて既存の「保存」ボタンでの一括反映方式に統合し、`updateRoomPositionsAction`を`saveFloorLayoutAction`に拡張（移動・追加・削除をまとめて1トランザクションで処理）
  - あわせて**セキュリティ修正**：このアクションが対象フロア・会議室が操作者の所属組織のものかを検証していなかった（マルチテナント化前は組織が1つしかなく問題が表面化しなかった）。フロア図アップロードAPIと同じ「組織所有チェック」パターンを追加した
- [x] **リサイズ機能**（`feature/room-resize`ブランチ）：会議室の右下角に■ハンドルを追加し、ドラッグで幅・高さを変更できるようにした（左上位置は固定、最小サイズでクランプ）。移動・追加・削除と同様、既存の「保存」ボタンでの一括反映方式に統合（`RoomPositionUpdate`にwidth/heightを追加、サーバー側でも最小サイズをクランプ）。実機確認済み
  - **バグ修正**：保存前（編集中）はviewBoxがサーバー保存済みデータのみを見て計算されていたため、大きくリサイズした際にキャンバスが追従せず見た目のサイズが不正確になる問題があった。displayRooms（編集中の状態を反映済み）を基準に計算するよう修正
- [x] **表示方式の見直し**：フロア図画像があるフロアは、画像を拡大縮小せず実ピクセルサイズのまま表示し、はみ出す分はスクロールする方式に変更（`overflow-auto`のコンテナ＋width/heightを画像サイズに固定したsvg）。画像が無いフロアは、従来どおり表示エリア自体を1フロア分としてaspect-ratio固定のコンテナ内で拡大縮小表示する。無理に固定比率へ画像を押し込めていたことがこれまでのサイズ不整合の根本原因だったため、この使い分けで解消した
- [x] **レイアウト変更**：フロアマップを画面横いっぱいに表示するよう変更し、予約状況（`RoomDetailPanel`）専用の右カラムを廃止。クリックした会議室の右隣（収まらなければ左隣）に、フロアマップ上へ重ねるポップオーバーとして表示するようにした（`getScreenCTM()`で会議室のSVG座標を画面座標に変換）。`RoomDetailPanel`に`onClose`を追加し閉じるボタンを表示。編集モードの「＋会議室を追加」等のパネルはマップ下に全幅で表示するよう変更
  - ポップオーバーは、フロアマップの枠（`overflow-hidden`/`overflow-auto`）からはみ出しても見切れないよう、マップ本体の外（スクロールしない側のラッパー）に配置している。位置は「現在ビューポート上のどこに見えているか」を基準に計算し、フロア図画像のフロアはマップをスクロールするたびに再計算して追従させる
  - フロア図画像のスクロールコンテナは、見た目のスクロールバーを`no-scrollbar`ユーティリティ（`globals.css`）で非表示にした（スクロール自体は有効なまま）
  - スクロール追従は自動テスト環境（プログラムによる`scrollLeft`代入は`scroll`イベントが発火しないため）では検証しきれなかったが、実機で動作確認済み
- [x] **フロア図画像の自動リサイズ**（`feature/floor-plan-resize`ブランチ）：長辺が1920pxを超える画像はアップロード時にsharpで自動縮小するようにした（縦横比維持、拡大はしない）。既存の会議室がある状態で画像サイズが変わる差し替えの場合、差し替え前後のサイズ比で全会議室の位置・サイズを比例配分して補正する。`image-size`パッケージはsharpに一本化して削除。実際に3Fの本番データ（3840×2160→1920×1080）で動作確認済み（既存4会議室の座標がすべて正確に半分になることを確認）
- [x] **バグ修正**：画像を差し替える（再アップロードする）たびに新しいタイムスタンプ付きパスで保存する一方、旧パスの画像がSupabase Storageに残り続け、ゴミが溜まり続けていた（自動リサイズの導入で再アップロードが増えたことで顕在化）。組織所有チェック用に取得済みのフロア行に`floorPlanImagePath`を含めて差し替え前のパスを取得しておき、新画像のアップロード＋DBトランザクションが成功した後にベストエフォートで`supabase.storage.from(BUCKET).remove([oldPath])`により旧ファイルを削除するようにした（削除失敗はログのみでレスポンスには影響させない）

### 会議室検索（`feature/room-search`ブランチ）
- [x] 参加人数（定員以上）・開始/終了時刻（本日の予約との重複判定）で会議室を絞り込めるようにした。フロアは検索条件に含めず、既存のフロアタブで選択中のフロアの中だけで絞り込む（フロアタブと役割が重複しないようにするため）
  - サーバーに問い合わせず、フロアマップ表示時点で既に読み込み済みのデータ（会議室のcapacity・本日の予約）だけでクライアント側完結。重複判定は`reservationOverlap.ts`の`isOverlapping`（DB非依存のテスト済み純粋関数）をそのまま再利用した
  - 条件に合わない会議室はクリック不可にし、グレーアウト＋破線表示（`--room-muted`）で区別。合致した会議室は空色の枠で強調表示する
  - 検索で開始・終了時刻を指定していた場合、会議室クリック後の予約フォームにその時刻を自動入力するようにした（`RoomDetailPanel`/`BookingForm`に`initialBookingRange`を追加）
  - 「本日の予約」しか読み込んでいないため、検索も本日の時間帯のみが対象（日をまたぐ空き状況の検索は`feature/floor-map-date-navigation`ブランチで対応）

### フロアマップの日付ナビゲーション（`feature/floor-map-date-navigation`ブランチ）
会議室検索で未来の日付を検索欄（datetime-local）に直接入力できてしまうが、実際には「本日の予約」しか読み込んでおらず、正しい空き状況を反映していなかった（未来に実際は予約がある会議室でも「空き」と表示されてしまう）不具合を、フロアマップ自体に「表示する日付」という概念を導入することで根本から解消した。
- [x] `getFloorMapData`に`targetDate`引数を追加（省略時は今日）。「本日固定」だったハードコードを`targetDate`基準に変更し、`isOccupiedNow`（今まさに使用中か）は`targetDate`が今日の場合のみ計算するようにした（過去・未来の日付には「今」という概念が無いため）
- [x] トップページに`?date=YYYY-MM-DD`のURLパラメータを追加（`src/lib/dateKey.ts`に日付キーの変換・加算・表示ラベル生成のユーティリティを新設、純粋関数としてテストを追加）
- [x] `DateNav`コンポーネントを追加し、前週・前日・（日付を直接指定できる`<input type="date">`）・翌日・翌週・今日（今日を見ていない時のみ表示）のナビゲーションをフロアマップ画面に設置
- [x] 検索欄の開始・終了を、日付も入力できてしまう`datetime-local`から**時刻のみ**（`type="time"`）に変更し、日付は常にフロアマップが表示中の日付を使うようにした。これにより「検索欄に入れた日付が実際には無視される」という誤解を生む不具合を構造的に解消。検索欄の下に「（9月7日(月)の空き状況で絞り込みます）」のように対象日を明示するようにした
- [x] 「使用中/空き」の表示は、今日を見ている場合のみ意味を持つ概念のため、それ以外の日を見ている場合は「予約あり/空き」の表示に変更（会議室ポップオーバーのバッジ・フロアマップ上の会議室の色分け・ラベルの両方）
- [x] 予約フォームの開始・終了の初期値を、検索条件が無い場合「今日を見ている時は現在時刻を切り上げた次の30分単位」「それ以外の日を見ている時はその日の9:00」に変更（以前は表示中の日付に関わらず常に実際の現在時刻を基準にしていたため、未来の日付を見ながら予約すると初期値が実際の現在時刻のままという不整合があった）
- [x] リファクタ：`toDatetimeLocalValue`関数が`RoomDetailPanel.tsx`・`EditReservationForm.tsx`に重複定義されていたのを`src/lib/dateKey.ts`に集約
- [x] 実機で、前日・翌日・前週・翌週・直接日付指定それぞれでのフロアマップの再取得、未来日での会議室検索（実際に未来日に予約を作成し、その時間帯を検索すると正しく「条件外」になることを確認）、日付ごとのラベル出し分け（本日/9月7日(月)等）、予約削除までの一連の動作を確認済み

### 予約者モデルの見直し（`feature/reservation-owner-model`ブランチ）
カレンダー画面（ログインユーザー自身の予約を表示する）の設計相談の中で、`reservations.bookerName`が自由入力のテキストで、ユーザーIDと紐付いていないため「自分の予約」を正確に判定できないことが判明。カレンダー着手前に、この根本のデータモデルを直す方針とした。
- [x] **スキーマ変更**：`bookerName`（自由入力）を廃止し、`createdByUserId`（`users.id`参照、nullable）＋`note`（任意の備考欄、URL等も入力可）に置き換えた
  - `createdByUserId`は`onDelete: set null`にしている。ユーザーが組織から削除された後も、その予約自体（＝その時間は使用中という情報）は残したいため（cascadeにすると削除されてしまう）。表示は「予約者不明」にフォールバックする
  - マイグレーションは2段階に分けて`drizzle-kit push --force`を実行（①`createdByUserId`・`note`列を追加→②`bookerName`列を削除）。同じpushで型の近い列の追加・削除を同時に行うと、drizzle-kitが「リネーム」と誤って解釈するリスクがあるため
  - 既存の予約データ（日時・会議室・件名）はそのまま残した（列だけの変更のため、行を削除する必要は無かった）。予約者情報だけ「予約者不明」表示になる
  - `db:seed`のダミーデータもbookerName廃止に合わせて更新（その組織の既存ユーザー1人に予約者を紐付け、新規ユーザーは作成しない）
- [x] 予約フォームから「予約者名」の入力欄を廃止。予約者は作成時のログイン中ユーザーで固定（クライアントからの入力は信用せず、サーバー側で`member.id`を設定）。代わりに任意の「備考（URLも可）」欄を追加
- [x] 予約一覧の「予約者」表示は、ログイン中ユーザーの氏名（無ければメールアドレス）をDBから都度joinして表示するよう変更（自由入力ではなくなったため常に実際のアカウント名と一致する）
- [x] 実機で、既存データ（予約者不明表示）・新規予約作成時の予約者自動設定（DBの実際のユーザー名で表示されることを確認）・備考欄（URL）の保存と編集フォームでの表示を確認済み
- [x] **権限の変更**（`feature/reservation-owner-permissions`ブランチ）：予約の編集・削除を「予約者本人」または「管理者」のみに制限した
  - 予約者が組織から削除された予約（`createdByUserId`がnull）は本人が存在しないため、管理者のみが操作できる
  - サーバー側（`updateReservationAction`/`deleteReservationAction`）で判定するのに加え、権限が無い予約には「編集」ボタン自体を表示しないようにした（`RoomDetailPanel`/`FloorMapView`/`page.tsx`に`currentMemberId`・`isAdmin`を追加で通す）
  - 実機で、一般メンバーとして自分の予約にのみ「編集」が出ること・他人の予約や予約者不明の予約には出ないこと、管理者としてはすべての予約に「編集」が出て実際に他人の予約を編集・削除できることを確認済み

### カレンダー画面（`feature/calendar-screen`ブランチ）
自分や組織内の予約状況を一覧できる別画面が欲しいとの相談を受けて追加。ログインユーザー自身の予約を月表示のカレンダーに載せ、日付をクリックするとその日の会議室予約状況（組織内全会議室）を確認できるようにした。
- [x] 新しいページ`/calendar`を追加。月表示（`<input type="date">`ではなく`?month=YYYY-MM`のURLパラメータ、前月・翌月ナビ付き）
- [x] 各日付の枠には、ログイン中ユーザー自身の予約のみを簡潔に（時刻・会議室名）表示し、3件を超える分は「+N件」でまとめる。組織内の全予約をここに載せると情報過多になるため
- [x] 各日付の枠右下に「予約する」ボタンを配置し、クリックでその日付を指定してフロアマップ（`/?date=YYYY-MM-DD`）へ遷移する（`feature/floor-map-date-navigation`で作った日付パラメータの仕組みをそのまま利用）
- [x] 日付の枠自体をクリックすると、画面下部にその日の組織内全会議室の予約状況（フロア・会議室名・時刻・会議名・予約者名）を時刻順で表示する
- [x] `src/lib/queries/getCalendarMonthData.ts`を新設。取得範囲は暦月そのものではなく月表示グリッド（前月末・翌月頭のパディング日を含む、日曜始まりの週で埋めた範囲）に合わせている。これにより、グリッド上に薄く表示される前月末・翌月頭の日をクリックしても、その日の予約状況が正しく表示できる
- [x] `src/lib/dateKey.ts`に月キー（`YYYY-MM`）関連のユーティリティ（変換・加算・表示ラベル・月表示グリッドの日付配列生成）を追加。すべて純粋関数としてテストを追加
- [x] ページ間ナビ（`SiteNav`）を、これまで管理者にしか表示していなかったのを全メンバーに拡張し、「会議室マップ・カレンダー」を常に表示するように変更（管理者にはさらに「フロア管理・メンバー招待」を追加表示）
- [x] **バグ修正**：月を切り替えても、カレンダー画面の選択中の日付（Client Componentのstate）がリセットされず前月の日付を保持し続けてしまう不具合があった。`CalendarGrid`に`monthKey`を`key`として渡し、月が変わるたびに作り直すことで解消
- [x] 実機で、月表示・前月/翌月ナビ・日付クリックでの詳細表示切り替え（前月末・翌月頭のパディング日を含む）・「予約する」からフロアマップへの遷移・月切り替え時の選択日リセットを確認済み

### バグ修正（`fix/edit-reservation-form-scrollbars`ブランチ）
- [x] 予約の編集フォームに縦横スクロールバーが表示される問題を修正。
  - 横スクロール：`EditReservationForm.tsx`の開始・終了欄が、`BookingForm`で既に修正済みだった「横並び（flex-1×2）だとdatetime-local入力欄の最小幅に収まらずはみ出す」不具合と同じ状態のまま残っていた（当時この修正が横展開されていなかった）。同様に縦積みに変更して解消
  - 縦スクロール：予約一覧の`<ul>`に、行数が多いときのための高さ制限（`max-h-56 overflow-y-auto`）をかけていたが、編集フォームはこの高さに収まらず内側でスクロールしてしまっていた。編集中の予約IDを`ReservationRow`のローカルstateから`RoomDetailPanel`側に引き上げ、いずれかの行を編集中は`<ul>`の高さ制限自体を外すようにした（同時に編集できるのは1件のみのため、外してもパネルが際限なく伸びる心配は無い）
  - 実機で、編集フォームの各要素の`scrollWidth`/`scrollHeight`が`clientWidth`/`clientHeight`と一致し、スクロールバーが発生しないことを確認済み

### LP（ランディングページ）（`feature/landing-page`ブランチ）
Claude Design（キャンバスエディタ）で作成したLPデザイン案（`DesignSync`ツールでプロジェクトを読み込み、`/design-login`で認証）を実装した。デザインは「1a／マップ先行」「1b／ストーリー先行」の比較用2案があったが、実装するのは1bのみとし、プロダクト名は案の仮名「Roomap」をそのまま採用した。
- [x] 未サインインで`/`にアクセスすると、これまでは`/login`へ即リダイレクトしていたのを、LP（`LandingPage.tsx`）を表示するように変更。サインイン済みなら従来通り会議室マップを表示する
- [x] LPのCTA（「無料で試す」）はすべて`/signup`（組織のセルフサインアップ）へ、ヘッダーの「ログイン」リンクは`/login`へ遷移する
- [x] デザインのキャンバス固有の記法（`.dc.html`の`sc-if`/`sc-for`独自テンプレート・`DCLogic`クラスベースの状態管理）は、このアプリの他のコンポーネントと同じ素のReact（`useState`）＋Tailwindに書き直した。キャンバス側の一括切り替えタブ（案1a/1bの比較用UI）は本番には不要なため実装していない
  - `FaqAccordion.tsx`：1問だけ開くシンプルなアコーディオン（デザイン通り初期状態で1問目を開いておく）
  - `LandingFloorMap.tsx`：ヒーロー部分を飾る、架空の部屋名（さくら・かえで等）による静的なフロアマップイラスト（実データとは無関係、クリック等の操作は無し。デザインの1b案自体がこの部分を非インタラクティブにしていたため）
- [x] LP専用に`next/font/google`で「Zen Kaku Gothic New」を追加。既存のログイン後の画面（Geistのまま）には影響しないよう、ルートの`layout.tsx`ではなく`LandingPage.tsx`側でスコープして適用した
- [x] プロダクト名「Roomap」をブラウザタブのタイトル（`layout.tsx`）とREADMEにも反映
- [x] 実機で、LPの全セクション表示、FAQアコーディオンの開閉、各CTAの遷移先（`/signup`）、ログインリンク（`/login`）、サインイン済みユーザーには従来通り会議室マップが表示されることを確認済み

### ログイン・サインアップ画面のデザイン変更（`feature/login-redesign`ブランチ）
Claude Designの同じプロジェクトに追加されたログイン画面デザイン案（「2a／2カラム」「2b／中央1枚」）のうち、LP（1b）とのブランド連続性が高い2aを採用した（`github.md`の同期メモにも2aが実際の`/login`・`/signup`のコピーを元に作られたとある）。2aのGoogleボタンが黒一色でLPのミント/エメラルドのアクセントカラーと合っていなかった点を、LPの配色に合わせて修正した。
- [x] `AuthScreen.tsx`を新設し、`/login`・`/signup`で共通の2カラムレイアウト（左：ダーク背景＋説明コピー＋`LandingFloorMap`の静的イラスト、右：フォーム）として使い回す。実際の2ページは別ルート・別Server Actionのままなので、デザイン案にあった「同一画面内でのモード切り替え」ではなく、素の`<Link>`による画面遷移にした
- [x] `LandingFloorMap.tsx`にオプションの`className`を追加し、LPのヒーロー（下側のみ角丸）とログイン画面（四隅角丸のカード）の両方で使い回せるようにした
- [x] Googleボタンの配色を黒一色からLPの配色（`bg-emerald-700`）に変更し、ブランドの一貫性を持たせた
- [x] デザインにあった「エラー表示のオン/オフ切り替え」ボタン（動作確認用）は実装していない
- [x] **バグ修正**：右カラム（フォーム側）に明示的な背景色を指定しておらず、閲覧者のOSがダークモード設定の場合、`body`側のCSS変数（`prefers-color-scheme: dark`）がそのまま透けて意図せず暗転してしまっていた。`bg-white`を明示して固定配色にした（LPと同様、このスクリーンはOSのダークモード設定に追従させる意図が無いため）
- [x] 実機で、`/login`・`/signup`双方の表示、エラー表示時のレイアウト、モバイル幅（左カラムを非表示にし、フォームへ直接到達できるようにした）、切り替えリンクの遷移先を確認済み

### バグ修正（`fix/scrollbar-layout-shift`ブランチ）
- [x] 会議室をクリックして予約状況ポップオーバーが表示されると、ヘルプテキスト（「会議室をクリックすると...」）が消えてページの高さがわずかに変わり、ブラウザの縦スクロールバーが出たり消えたりしてコンテンツの見た目の幅がガタつく問題を修正。
  - 当初`html`に`overflow-y: scroll`（スクロールバーを常時確保）で対処したが、「そもそもスクロールバーが出ない状態にしたい」との要望を受けて方針転換：
    1. ヘルプテキストの行自体は選択の有無に関わらず常に描画し、中身の表示・非表示だけを`invisible`で切り替えるようにして、ページの高さ自体が変動しないようにした（根本原因への対処）
    2. フロア図画像の表示コンテナの高さを固定`480px`からビューポート比`60vh`に変更し、ウィンドウが小さい環境でもページ全体がなるべく収まるようにした
  - 実機で、会議室クリック前後で`document.documentElement.scrollHeight`が完全に一致し、通常サイズのウィンドウ（1280×800）ではスクロール自体が発生しないことを確認済み

### バグ修正（`fix/booking-form-end-input-offscreen`ブランチ）
- [x] 予約状況ポップオーバー内で「この部屋を予約する」をクリックすると、予約フォームの「終了」入力欄が画面外にはみ出す問題を修正。
  - 原因：ポップオーバーは幅300px固定だが、`BookingForm`の開始・終了は`flex`＋`flex-1`で横並びにしていた。flexアイテムはデフォルトで中身（datetime-local入力欄のブラウザネイティブ最小幅）より縮まないため、2つ並べると300px幅に収まりきらず、右側の「終了」欄がパネルの外へあふれていた
  - 横並びのまま`min-w-0`で縮めさせる案もあったが、日時ピッカーが見づらいほど圧縮される恐れがあるため、開始・終了を縦積みに変更し、それぞれ全幅を使わせることで確実にあふれを防いだ
  - 実機（`本社ビル`フロアの実データ）で、ポップオーバー内の開始・終了欄が画面内に収まることを確認済み

### 管理者用マスター管理画面（`feature/floor-management`ブランチ）
建物マスター管理は見送り、「1組織＝1建物」の前提は維持する方針とした（支社を持つ場合は別組織＝別管理者を立てる運用）。そのためフロア管理・ユーザー管理の2画面を追加する。
- [x] **フロア管理画面**（`/admin/floors`）：これまでフロアの新規作成手段がオンボーディング時の最初の1枚しかなく、以降の追加・改名・削除ができなかった
  - フロア一覧（フロア番号順、会議室数・予約件数を併記）／追加（フロア番号・表示名）／編集（インライン）／削除
  - フロア番号は建物内で重複できないようアプリ側で検証（`floors`テーブルに一意制約は無いため、`createFloorAction`/`updateFloorAction`側でチェック）
  - 削除はDB側の`onDelete:cascade`で会議室・予約も連鎖削除されるため、確認ダイアログに「会議室○件・予約○件も削除される」旨を明示（予約件数は本日分に限らず全期間で集計）
  - 他組織のfloorIdを直接指定されても操作できないよう、既存の「組織所有チェック」パターン（`isFloorInOrganization`）を追加
  - 管理画面が複数になったため、共通の`AdminNav`コンポーネントを追加し各管理画面の上部・トップページのヘッダーからリンクできるようにした
  - 実機で、フロアの追加・編集（フロアタブへの反映まで確認）・重複フロア番号のエラー表示・削除確認ダイアログの文言・削除の連鎖反映を確認済み
- [x] **ユーザー管理画面の拡張**（`feature/user-management`ブランチ、`/admin/invitations`）：招待作成＋一覧表示のみだったところに、メンバーごとのロール変更・削除を追加した
  - ロール変更（管理者⇔一般メンバー）・削除（`invited`のまま未サインインの招待を取り消す、`active`なメンバーを組織から外す）は同じ`users`行に対する操作で、削除は行自体を消すだけでよい（認可はこの行の有無で判定しているため、以後サインインできなくなる）
  - **自分自身に対するロール変更・削除は不可**にした。「他の管理者を降格・削除する」操作自体は許可しているが、実行者は必ず管理者のまま残るため、この制約だけで組織の管理者が0人になる事故を防げる（個数を数える必要がない、シンプルで確実な方式）
  - 他組織のmemberIdを直接指定されても操作できないよう、既存の「組織所有チェック」パターンを追加
  - 招待メール送信の仕組みが無い（許可リスト方式）ため「再送」機能は不要と判断
  - 実機で、ロール変更の反映・自分自身の行にボタンが出ないこと・削除確認ダイアログの文言（招待中／参加済みで文言を出し分け）・招待の取り消しを確認済み

### 共通ヘッダー・アカウントメニュー（`feature/app-header`ブランチ）
管理画面（フロア管理・メンバー招待）に入ると会議室マップへ戻る導線が無く、しかもログイン中の自分が誰か・ログアウトボタン自体もマップ画面にしか無かった。あわせて、ヘッダーのメールアドレス表示をGoogleアカウントのアバター表示に変更する提案も実装した。
- [x] 会議室マップ・フロア管理・メンバー招待の3画面共通の`AppHeader`コンポーネントを新設し、各ページの個別ヘッダー実装を置き換えた
  - 管理者には常に3画面へのページ間ナビ（`SiteNav`、現在地を強調表示）を表示するようにし、管理画面から会議室マップへ戻れない問題を解消
- [x] ヘッダーのメールアドレス表示を、Googleアカウントのアバター画像（`AccountMenu`）に変更した
  - Auth.jsが標準で`session.user.image`に積んでいるGoogleプロフィール画像URLをそのまま使う（DBへの保存やGoogle側のスコープ追加は不要）。`next/image`で表示するため`next.config.ts`に`lh3.googleusercontent.com`を許可ドメインとして追加
  - 画像が無い場合は氏名/メールアドレスの頭文字を丸背景で表示するプレースホルダーにフォールバック
  - 「ホバーで表示」ではなく**クリック（タップ）で開閉**する方式にした。ホバー状態はタッチ操作（スマホ・タブレット）に存在せず、その中に移すログアウトボタンに永久に手が届かなくなる恐れがあるため
  - ポップオーバー内に氏名・メールアドレス・ログアウトボタンをまとめ、単体の「サインアウト」リンクは撤去した
  - **バグ修正**：`AccountMenu`（Client Component）から`LogoutButton`（Server Component、内部でAuth.js/DBに依存するServer Actionを持つ）を直接importして描画すると、Client Componentのバンドル対象に取り込まれてしまい、postgres等のNode専用モジュール解決に失敗してビルドが落ちる問題があった。`LogoutButton`をchildrenとして呼び出し側（Server Componentの`AppHeader`）から渡す構成に変更して解決
  - 実機で、3画面すべてでの表示・「会議室マップ」リンクでの復帰・メニューの開閉（外側クリックで閉じる）・アバター無し時のプレースホルダー表示を確認済み
  - **バグ修正**：実機確認で、アカウントメニューにメールアドレスは表示されるが氏名が表示されない不具合を発見。原因は`src/auth.ts`側：招待経由のメンバーは`users`行作成時（`createInvitationAction`）に名前を入力する手段が無く、サインイン時にもGoogleプロフィールの`name`を反映する処理が存在しなかった（新規組織オーナーのセルフサインアップ時にしか`name`を保存していなかった）ため、招待メンバーの`name`は常にnullのままだった。既存メンバーのサインイン時にも、Googleプロフィールの`name`とDBの値が異なれば都度同期するよう修正。次回サインイン時から反映される（JWT戦略のため、サインインし直すまで今のセッションには反映されない）

## 優先度：低（余力があれば）

- [ ] 実DB（Supabase）を使った結合テスト（Vitest導入計画のステップ4）

## スコープ外（README「今後の展望」・stretch goals）

- [ ] Google Calendar APIとの実連携（読み取り／書き込み）
- [ ] 部屋・機能単位のより細かい権限設定
